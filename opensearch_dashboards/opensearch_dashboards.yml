---
# DEV-FIRST configuration: no TLS, no auth.
# Anything not listed here uses the upstream default.

server.host: "0.0.0.0"
server.name: log4opensearch-dashboards

opensearch.hosts: ["http://opensearch:9200"]
opensearch.ssl.verificationMode: none

# The security plugin is removed from the image, so no security settings are needed here.
