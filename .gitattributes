# Shell scripts run on Linux (CI, containers). A CRLF here means `bad interpreter: bash^M`,
# so pin them to LF regardless of the contributor's core.autocrlf.
*.sh          text eol=lf
*.conf        text eol=lf
*.yml         text eol=lf
Dockerfile    text eol=lf

*.png         binary
*.xpz         binary
